<!DOCTYPE html>
<html>
    <head>
            <title>Editar Usuario</title>
    </head>

    <body>
    <h1 class="header">Editar Usuario</h1>

    <form action="/usuario/editar" method="post">
        <input id="id-usuario" type="hidden" name="id" value="{{usuario.id}}"/>
        <h2>Edit User</h2>
        <div class="field">
            <label>Nombre </label>
            <input id="nombre-usuario" type="text" name="nombre" value="{{usuario.nombre}}" placeholder="El nombre" required/>
        </div>
        <br>
        <div class="field">
            <label>Género</label>
            <input id="email-usuario" type="email" name="email" value="{{usuario.email}}" placeholder="El email" required/>
        </div>
        <p>
            <button onclick="location.href='/usuario/detalles/{{usuario.id}}'; return true;">Cancel</button>
            <input type="submit" id="Save" value="Save" />
        </p>
    </form>

    </body>
</html>